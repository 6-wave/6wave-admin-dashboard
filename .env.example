# Base URL of the ASP.NET Core API. Leave empty to call the same origin.
VITE_API_BASE_URL=

# Demo mode: made-up data and public demo passwords, all in the browser.
# On by default in `bun run dev`, off in production builds.
# NEVER set this to true on a deployment that holds real data.
VITE_USE_MOCKS=
